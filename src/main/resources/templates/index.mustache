{{> header}}

<h1>{{title}}</h1>

<div class="articles">

    {{#articles}}
        <section>
            <header class="article-header">
                <h2 class="article-title"><a href="/article/{{slug}}">{{title}}</a></h2>
                <div class="article-meta"><strong>{{addedAt}}</strong></div>
            </header>
            <div class="article-description">
                {{content}}
            </div>
        </section>
    {{/articles}}
</div>

{{> footer}}